var assert = require("assert");
var th = require("../testhelper").TestHelper;
var Db = require("../../server/tools/db").Db;

describe('API datatypes', () => {

    describe('POST/', () => {

        xit('responds with 400 when name is not allowed ("allusers", "clients", "datatypefields", "datatypes", "permissions", "pg_*", "usergroups", "users")', async() => {});

    });

});
